import React from 'react'
import { Button } from 'react-bootstrap'

const ClosePullRequestButton = ({ pullRequestIds }) => {
  return <Button variant='danger'>Delete Selected Pull Requests</Button>
}

export default ClosePullRequestButton
