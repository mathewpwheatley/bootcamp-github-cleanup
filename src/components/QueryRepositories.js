import React from 'react'
import { useQuery } from '@apollo/client'
import { GET_ALL_REPOSITORIES } from '../operations/queries/getAllRepositories'

const QueryRepositories = () => {
  return <div></div>
}

export default QueryRepositories
